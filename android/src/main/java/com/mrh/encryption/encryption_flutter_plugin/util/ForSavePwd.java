package com.mrh.encryption.encryption_flutter_plugin.util;

import android.content.Context;

import java.security.Key;

public class ForSavePwd {
//    static  String forSavePwd(String pwd,String key) throws Exception {
//        //初始化密钥
//        byte[] key = DESCodec.initSecretKey(Context);
//        //秘钥转换
//        Key k = DESCodec.toKey(key);
//        //加密
//        byte[] encryptData = DESCodec.encrypt(pwd.getBytes(), k);
//        String hexData = Hex.encode(encryptData);// 二进制转换成16进制数据 保存的数据
//        return hexData;
//    }
}
